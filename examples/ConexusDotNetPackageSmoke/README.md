# ConexusDotNetPackageSmoke

Compile-only consumer that references the same Ontogony set as [`ConexusDotNetSkeleton`](../ConexusDotNetSkeleton/) via **packed** `PackageReference` entries. Used by CI after `scripts/pack-all.ps1` (PR-PLAT-002). See the skeleton README for commands.
