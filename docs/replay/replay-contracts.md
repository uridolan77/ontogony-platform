# Replay contracts

`Ontogony.Replay.Contracts` describes replay bundles for deterministic debugging and incident reconstruction.

It does not execute replays. It records enough structure to later say: these were the inputs, expected outputs, environment facts, deterministic hints, and artifacts.
