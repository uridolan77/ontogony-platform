# Prompt — Compatibility Harness

Produce a local compatibility report proving frontend/backend alignment. Minimum proof: Allagma health OK, OpenAPI snapshot hash, frontend snapshot hash, audit bundle includes `SandboxEvidence`, frontend adapter tests pass, sandbox evidence fixture renders, real external execution blocked state visible, local sandbox execute state visible, no raw content displayed.
