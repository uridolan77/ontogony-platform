# Prompt — Frontend Sandbox Evidence Bridge

Repo: `uridolan77/ontogony-frontend`

Implement frontend consumption of Allagma Phase 3 sandbox evidence. Refresh Allagma OpenAPI/client snapshot, add typed adapter, extend run audit/replay UI, render the five sandbox execute events, add fixtures, add tests, and keep all checks green. No raw marker content. No fake backend capability. ReplaySkipped is safe idempotency evidence, not an error.
