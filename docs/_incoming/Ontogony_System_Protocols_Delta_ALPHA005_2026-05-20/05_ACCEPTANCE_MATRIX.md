# Acceptance matrix

| Issue | Required result | Evidence artifact |
|---|---|---|
| `SYS-OBS-004A` | verify-system-observability.ps1 -UseExistingServices returns PASS against Docker-local stack; artifact is committed or indexed under evidence; B-012 is marked cleared in Allagma and platform evidence indexes; no production readiness claim is added. | Named evidence doc or generated artifact linked in repo evidence index |
| `SYS-LOCK-006` | validate-runtime-lock.ps1 -RequireEvidence -ReleaseMode PASS for new lock; system cohesion summary PASS; restart summary PASS; observability B-012 either PASS or explicitly quarantined with rationale; package/evidence indexes agree on the baseline label. | Named evidence doc or generated artifact linked in repo evidence index |
| `SYS-PROTOCOL-REGISTRY-001` | Registry validates against schema; each referenced file exists; registry can answer: what route/auth/env/error/evidence artifacts define this protocol surface? CI or script fails on missing evidence, stale baseline labels, obsolete package references, or dead paths. | Named evidence doc or generated artifact linked in repo evidence index |
| `SYS-STALE-PACKAGE-GUARD-001` | Validator reports stale items in old packages; new packages must include CURRENT_BASELINE.md, MOVING_MAIN_DELTA.md, and SUPERSEDED_ITEMS.md; CI/documented runbook prevents copying stale package content into canonical docs without reconciliation. | Named evidence doc or generated artifact linked in repo evidence index |
| `SYS-CONNECT-MATRIX-AUDIT-001` | A generated test or script fails if a matrix route is absent from the source inventory. Matrix rows distinguish direct HTTP call, typed client abstraction, indirect data dependency, and frontend-only link. Known gaps remain explicit, not implied connected. | Named evidence doc or generated artifact linked in repo evidence index |
| `SYS-E2E-REVALIDATE-006` | A single evidence bundle records PASS/FAIL for each required scenario, with exact repo SHAs and artifact paths. Any failure becomes an explicit quarantine, not hidden under Alpha-005 evidence. | Named evidence doc or generated artifact linked in repo evidence index |
| `SYS-EVIDENCE-INDEX-006` | A reader can determine from index alone: current cut baseline, current moving-main status, open quarantines, closed quarantines, companion FE/UI commits, and exact commands to validate. | Named evidence doc or generated artifact linked in repo evidence index |
| `KANON-CONNECT-LOCK-001` | Next closeout file includes KANON-CONNECT 001-007 status table, route parity status, Docker cross-service smoke status, and evidence spine semantic graph status. | Named evidence doc or generated artifact linked in repo evidence index |
| `CONEXUS-EVIDENCE-FLOW-001` | Given a modelCallId from Allagma or Kanon assistance, operator UI can resolve route evidence, admin detail, evidence links, evidence bundle, route decision detail, and usage/cost without raw prompt/completion leakage. | Named evidence doc or generated artifact linked in repo evidence index |
| `CONEXUS-POSTLOCK-001` | Each changed file group is classified; lock-impacting changes are either validated into Alpha-006 or left as moving-main deltas. Frontend/API clients know which new endpoints are safe to consume. | Named evidence doc or generated artifact linked in repo evidence index |
| `ALLAGMA-RUNTIME-POSTURE-001` | Frontend/system dashboard can show Allagma posture from live API: persistence, model aliases, streaming flags, real execution disabled, sandbox posture, downstream configured flags, and service/package version info. | Named evidence doc or generated artifact linked in repo evidence index |
| `SYS-REAL-TOOLS-BLOCK-VERIFY-001` | Default CI has a named safety test that fails if real execution becomes enabled by default or if docs overclaim real side-effect readiness. | Named evidence doc or generated artifact linked in repo evidence index |
