# TASK-008 — Shared UI Status Taxonomy

## Goal

Add/extend status primitives in `@ontogony/ui`.

## Components

```text
ConnectivityBadge
ReadinessBadge
ContractHealthBadge
DataSourceBadge
CompatibilityBadge
AuthorityBadge
```

## Acceptance

Frontend uses consistent status words and styling across Home, Settings, Topology, and Release Readiness.
