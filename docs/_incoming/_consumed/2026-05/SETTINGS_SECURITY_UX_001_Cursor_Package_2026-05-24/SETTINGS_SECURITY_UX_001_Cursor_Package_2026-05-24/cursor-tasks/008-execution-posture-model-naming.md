# Cursor Task 008 — Execution Posture and Model Naming

## Goal

Tighten execution posture and model-purpose/alias language.

## Steps

1. Add compact posture badges.
2. Add kill-switch severity/scope.
3. Avoid repeating full posture text across pages.
4. Search for ambiguous `model` copy.
5. Use `Model purpose`, `Conexus alias`, `Provider model` labels.

## Acceptance

- Real external remains blocked.
- Kill switch not configured is a local-alpha warning.
- Purpose/alias/provider model are distinct.
