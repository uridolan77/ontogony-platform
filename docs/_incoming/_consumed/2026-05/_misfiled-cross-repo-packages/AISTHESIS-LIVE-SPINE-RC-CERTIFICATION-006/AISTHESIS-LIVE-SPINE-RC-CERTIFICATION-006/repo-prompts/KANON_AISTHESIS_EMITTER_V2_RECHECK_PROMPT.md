# Cursor prompt — Kanon Aisthesis emitter v2 recheck

Apply inside `C:\Dev\kanon-dotnet` only if Aisthesis certification reports missing Kanon edges.

Check that Kanon emits or can emit:

- decision -> semantic plan;
- decision -> policy;
- decision -> canonical fact where applicable;
- decision -> source binding where applicable;
- human gate opened -> resolved;
- decision -> replay bundle where applicable.

Kanon remains semantic authority. Aisthesis only records evidence.
