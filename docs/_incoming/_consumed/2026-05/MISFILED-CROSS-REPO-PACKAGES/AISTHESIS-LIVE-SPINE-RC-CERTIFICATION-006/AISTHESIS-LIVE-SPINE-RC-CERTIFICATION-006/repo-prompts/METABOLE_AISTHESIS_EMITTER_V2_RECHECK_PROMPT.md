# Cursor prompt — Metabole Aisthesis emitter v2 recheck

Apply inside `C:\Dev\metabole-dotnet` only if Aisthesis certification reports missing Metabole edges.

Check that Metabole emits or can emit:

- pipeline -> schema extract;
- pipeline -> data profile;
- pipeline/profile -> mapping candidate;
- mapping candidate -> review;
- mapping candidate/review -> artifact;
- transformation plan -> output artifact;
- mapping candidate -> Kanon source binding proposal where applicable.

Do not move transformation semantics into Aisthesis.
