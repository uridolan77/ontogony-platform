# Cursor prompt — Phase 0 reality audit

Audit current SYSTEM-COH state across sibling repos. Do not edit yet.

Produce `allagma-dotnet/docs/evidence/SYSTEM_COH_001_CURRENT_STATE_AUDIT.md` with:

- current repo refs;
- existing cohesion docs/scripts/tests;
- implemented vs missing package items;
- stale gap warnings;
- recommended exact edits.

Check especially whether model-purpose aliasing, runtime lock, trace/context matrix, system cohesion smoke, streaming purpose support, package-mode build, real-tools blocking docs, and Evidence Spine enriched roots already exist.
