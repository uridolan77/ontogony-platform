# Cursor prompt — Phase 2 smoke/evidence runner

Add `scripts/validate-system-coh-001.ps1` and `scripts/run-system-coh-001-acceptance.ps1`.

Reuse existing smoke helpers. The runner should write `artifacts/system-coh-001/<timestamp>/summary.json` with schema `ontogony-system-cohesion-summary-v1`.

Add tests for matrix validity and required docs.
