# Repo task — ontogony-ui

UI work is optional for SYSTEM-COH-001.

## Required posture

Do not add new generic UI primitives unless frontend implementation requires them.

## Possible small additions

- Cohesion status badge type.
- Cross-service evidence status row.
- Shared deferral/status vocabulary if current status components are insufficient.

## Done when

Either:

1. No UI package change is needed and the closeout explicitly says why; or
2. Minimal shared UI primitive additions are documented and tested.
