# Scorecard targets

| Repo | Required improvements to justify >9 implementation depth |
| --- | --- |
| `ontogony-platform` | Complete high-value partial mechanics and prove consumer adoption through conformance harnesses. |
| `conexus-dotnet` | Mature provider capability enforcement, streaming posture, maintenance-job durability, fallback/routing evidence, and persistence proof. |
| `kanon-dotnet` | Close route/client truth drift, deepen decision lifecycle and domain-pack governance, add semantic graph/Postgres proof, widen safe client coverage. |
| `allagma-dotnet` | Close replay gaps, persist replay records, orchestrate Conexus route-decision dry-run, automate topology E2E, formalize streaming governed path, deepen MAF path. |

A repo does not reach 9+ by adding docs alone. Each repo needs a meaningful code/runtime slice, tests, evidence, current-state update, and known-limitations cleanup where applicable.
