# Allagma cross-repo closeout prompt

After all four repo slices have landed, work in `allagma-dotnet` to close `ONTOGONY-IMPLEMENTATION-DEPTH-OVER9-001`.

Tasks:

1. Refresh/validate runtime lock only after confirming latest compatible repo commits.
2. Run cross-repo conformance, feature matrix, system cohesion, governed fake replay, and governed MAF evidence checks.
3. Update `docs/system/SYSTEM_COMPATIBILITY_MATRIX.md` with actual current counts.
4. Reset or shrink `post-lock-deltas.json` according to the new baseline.
5. Create `docs/reviews/IMPLEMENTATION_DEPTH_OVER9_CLOSEOUT_REPORT.md` with a score justification for each repo.
6. Do not claim production readiness.
