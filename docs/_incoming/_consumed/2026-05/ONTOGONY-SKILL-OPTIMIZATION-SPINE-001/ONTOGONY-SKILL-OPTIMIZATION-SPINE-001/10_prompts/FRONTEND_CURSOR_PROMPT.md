# Frontend/UI Cursor Prompt

Implement the Skill Lab for `ONTOGONY-SKILL-OPTIMIZATION-SPINE-001`.

1. Inspect existing console pages, Evidence Spine components, Allagma/Kanon/Conexus detail pages, and `ontogony-ui` component exports.
2. Add compact reusable UI components: skill card, version badge, lifecycle badge, gate score panel, edit diff card, rejected buffer, deployment binding card, evidence links list.
3. Add Skill Inventory and Skill Detail views.
4. Add Optimization Run view with timeline, edits, gate, rejected buffer, and evidence links.
5. Link from existing run/model-call/decision/evidence pages without adding clutter.
6. Use deterministic fixture data first if backend routes are not complete.
7. Add tests for accepted, rejected, deployed, rollback, and empty states.

Prioritize operator clarity over showing every raw field.
