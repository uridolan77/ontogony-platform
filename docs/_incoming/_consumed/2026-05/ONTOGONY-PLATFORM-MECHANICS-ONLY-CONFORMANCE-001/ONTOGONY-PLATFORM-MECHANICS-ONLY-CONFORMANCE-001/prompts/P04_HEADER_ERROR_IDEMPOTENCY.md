# P04 — Header, error, idempotency checks

Implement focused checks for:

- frozen header propagation;
- error envelope shape;
- idempotency state mechanics.

Prefer fixture/static mode first. Add live mode only where safe and non-secret.
