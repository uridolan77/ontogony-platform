# SYSTEM-ALPHA-008 — Conexus capacity baseline evidence stub

This file is a placeholder for scheduled full-system cohesion closeout.

Expected command:

```powershell
$env:CONEXUS_RUN_CAPACITY_BASELINE = "true"
./scripts/capacity/run-capacity-baseline.ps1
```

Required artifacts:

- JSON capacity report;
- Markdown capacity report;
- threshold file version;
- scenario verdicts;
- explicit statement that this is local alpha/fake-provider capacity evidence, not production SLA proof.
