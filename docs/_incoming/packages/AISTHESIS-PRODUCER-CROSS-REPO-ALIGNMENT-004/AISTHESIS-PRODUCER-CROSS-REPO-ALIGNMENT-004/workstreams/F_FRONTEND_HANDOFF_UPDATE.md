# Frontend handoff update

Frontend remains handoff-only until a real live trace bundle exists.

After live proof, update handoff with:
- live trace ID;
- bundle shape;
- producer and identifier summaries;
- required-edge diagnostics examples;
- empty/error states.
