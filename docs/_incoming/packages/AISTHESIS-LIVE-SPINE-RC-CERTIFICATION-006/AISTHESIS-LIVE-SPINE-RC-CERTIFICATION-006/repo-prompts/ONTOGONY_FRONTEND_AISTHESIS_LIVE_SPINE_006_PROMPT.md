# Cursor prompt — Frontend Aisthesis live spine 006

Apply inside `C:\Dev\ontogony-frontend` after Aisthesis routes are stable.

Goal: validate frontend live evidence-spine backing.

Implement or verify UI/API support for:

- trace lookup;
- timeline display;
- graph display;
- reconstructability v2 report;
- required-edge status table;
- missing/ambiguous edge diagnostics;
- suggested producer fix;
- bundle export;
- auth/config state.

Create closeout:

```text
docs/evidence/ONTOGONY_FRONTEND_AISTHESIS_LIVE_SPINE_006_CLOSEOUT.md
```

Do not claim live backing unless tested against running Aisthesis.
