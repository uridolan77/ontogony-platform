# Cursor prompt — Allagma Aisthesis emitter v2 recheck

Apply inside `C:\Dev\allagma-dotnet` only if Aisthesis certification reports missing Allagma edges.

Check that Allagma emits or can emit:

- run lifecycle evidence;
- run -> semantic plan;
- run -> Kanon decision;
- run -> Conexus model call;
- run -> human gate when applicable;
- run -> tool intent;
- tool intent -> tool execution;
- tool execution -> side effect when real execution occurs;
- side effect -> output artifact when applicable.

Do not add Aisthesis ownership logic. Emit evidence only.
