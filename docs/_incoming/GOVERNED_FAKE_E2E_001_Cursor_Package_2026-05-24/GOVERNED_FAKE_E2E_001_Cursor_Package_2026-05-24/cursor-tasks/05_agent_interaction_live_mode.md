# Task 05 — Agent Interaction live mode

Default to live lookup when backend is available. Make fixture replay explicitly demo-only.

Acceptance:
- latest live governed fake run can be loaded;
- fixture mode is clearly non-authoritative.
