# Cursor prompt — aisthesis-dotnet (BACKEND-COORDINATION-002)

**Package:** `ONTOGONY-BACKEND-COORDINATION-002` slice 7 primary.

## Tasks

1. Merge `AISTHESIS-LIVE-FIVE-SERVICE-PASS-009` into slice 7 if still active.
2. Fix `global.json` SDK policy.
3. Run **live** five-service certification — not fixture-only.
4. Produce evidence with `reconstructabilityGrade: complete`.
5. Optional `docs/system/` matrices.

## Pass gate

```json
{ "mode": "Live", "status": "PASS", "requiredEdges": { "missing": 0 } }
```

## Boundary

Evidence/reconstructability only — not semantic truth or execution.

## Read

`repo-tasks/aisthesis-dotnet.md` + prompt P07.
