# Cursor prompt — kanon-dotnet (BACKEND-COORDINATION-002)

**Package:** `ONTOGONY-BACKEND-COORDINATION-002`

## Tasks

1. Docs order (slice 1).
2. Refresh compatibility manifest; resolve OpenAPI baseline drift (slice 2).
3. Error envelope for middleware paths; document typed DTO exceptions (slice 3).
4. Kanon.Client propagation (slice 4).
5. Participate in five-service smoke (slice 6).

## Coordination

If `KANON-SOURCE-BINDING-GOVERNANCE-HTTP-WIRING-004` is active, coordinate route changes with slice 2.

## Boundary

Semantic authority only — no provider SDKs, no workflow execution.

## Read

`repo-tasks/kanon-dotnet.md` + prompts P02, P03, P04.
