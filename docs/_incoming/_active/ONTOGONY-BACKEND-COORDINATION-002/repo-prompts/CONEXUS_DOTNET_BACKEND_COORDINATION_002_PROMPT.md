# Cursor prompt — conexus-dotnet (BACKEND-COORDINATION-002)

**Package:** `ONTOGONY-BACKEND-COORDINATION-002` slice 5 co-owner.

## Tasks

1. Fix Release build if failing.
2. Publish `CONEXUS_MODEL_ALIAS_CONSUMER_CONTRACT_V0` to `docs/contracts/`.
3. Error envelope middleware alignment (slice 3).
4. Propagation on provider HTTP clients (slice 4).
5. Export alias manifest for Allagma snapshot validation.

## Boundary

You own model access — not ontology or agent execution.

## Read

`repo-tasks/conexus-dotnet.md` + prompts P03, P05.
