# Allagma Cursor Prompt

Implement Allagma orchestration for `ONTOGONY-SKILL-OPTIMIZATION-SPINE-001`.

1. Inspect current run lifecycle, operation semantics, replay runtime, human gates, and route inventory.
2. Add a `SkillOptimization` run type or equivalent metadata.
3. Implement deterministic fixture orchestration: rollout evidence, optimizer edit generation, validation call, held-out gate, accept/reject, publish/deploy hooks.
4. Add run timeline and operations projections.
5. Add accepted and rejected candidate fixtures.
6. Add tests for operation availability, run lifecycle, replay, and evidence links.
7. Update docs and route inventories.

Keep the first slice fake-provider compatible and deterministic.
