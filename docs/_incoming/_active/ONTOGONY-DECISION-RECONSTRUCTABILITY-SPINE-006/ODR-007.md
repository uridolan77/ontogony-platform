Below are full Cursor prompts. I would use them in this order: **platform package**, then **Kanon implementation**, then **frontend Evidence Spine wiring**, then **verification/closeout**.

## Prompt 1 — Create DEC-RECON-007 package in `ontogony-platform`

```text
You are working in C:\dev\ontogony-platform.

Create the active package folder for:

    ONTOGONY-DECISION-RECONSTRUCTABILITY-SPINE-007

Purpose:
DEC-RECON-007 turns on-demand reconstructability classification into persisted, versioned, hash-addressable reconstructability report artifacts.

Current state:
- DEC-RECON-004 is closed: live stack proved Allagma decision-events → Kanon classify-batch → frontend panel.
- DEC-RECON-006 is closed: Evidence Spine now appends allagma.decisionEvent nodes with PASS/WARN/FAIL reconstructability overlay and opens ReconstructabilityReportPanel.
- Remaining gap: the system can show what Kanon says now, but cannot prove what Kanon said then, from which exact input, under which classifier version.

Create:

    docs/_incoming/_consumed/2026-05/ONTOGONY-DECISION-RECONSTRUCTABILITY-SPINE-007/README.md
    docs/_incoming/_consumed/2026-05/ONTOGONY-DECISION-RECONSTRUCTABILITY-SPINE-007/00_IMPLEMENTATION_PROMPT.md
    docs/_incoming/_consumed/2026-05/ONTOGONY-DECISION-RECONSTRUCTABILITY-SPINE-007/01_ACCEPTANCE_CHECKLIST.md
    docs/_incoming/_consumed/2026-05/ONTOGONY-DECISION-RECONSTRUCTABILITY-SPINE-007/02_ARTIFACT_CONTRACT.md
    docs/_incoming/_consumed/2026-05/ONTOGONY-DECISION-RECONSTRUCTABILITY-SPINE-007/03_VERIFICATION_PLAN.md

Package goal:
When a DecisionEventContract is classified by Kanon, the caller can request a persisted reconstructability report artifact containing:

- artifact schema
- artifact id
- decisionEventId
- decisionKind
- serviceOfOrigin
- sourceRef
- decisionEventHash
- canonical input payload or canonical input snapshot
- classifierVersion
- classifiedAtUtc
- traceId / runId / correlationId where available
- source identifiers / related IDs
- ReconstructabilityReportContract payload
- governance status and strict completeness summary
- report contract version
- safe reasoning notice
- createdAtUtc / updatedAtUtc if stored

Canonical artifact schema:

    ontogony-reconstructability-report-artifact-v1

Required implementation targets:

Kanon:
- Add artifact contract(s).
- Add deterministic canonical hashing for inbound DecisionEventContract.
- Add classifier version constant/service.
- Add persistence/repository for reconstructability report artifacts using existing Kanon persistence patterns where available.
- Add API endpoint(s) for generating/storing artifacts and fetching them.
- Preserve existing classify/classify-batch endpoints exactly; do not break DEC-RECON-004 or DEC-RECON-006.

Frontend:
- Evidence Spine allagma.decisionEvent nodes should be able to display artifact metadata when available.
- Keep current on-demand classify-batch path working.
- Prefer a progressive enhancement: if artifacts exist or artifact endpoint succeeds, show artifact id / decisionEventHash / classifierVersion / classifiedAtUtc.
- If artifact endpoint is unavailable, preserve current report panel behavior.

Platform:
- Add smoke/verification scripts or docs for validating DEC-RECON-007.
- Save evidence under docs/evidence or platform artifacts following current conventions.

Acceptance checklist:
- Artifact generated for at least one Allagma decision event.
- Artifact includes deterministic decisionEventHash.
- Same canonical input produces the same decisionEventHash.
- Artifact records classifierVersion.
- Artifact stores/returns the full report payload.
- Artifact fetch by artifactId works.
- Artifact fetch by decisionEventId works, or a documented indexed query equivalent exists.
- Existing classify/classify-batch tests still pass.
- DEC-RECON-004 smoke still passes.
- DEC-RECON-006 Evidence Spine still renders.
- New tests cover hash stability, artifact creation, artifact retrieval, and batch artifact generation if implemented.

Important design constraint:
Do not store or expose hidden chain-of-thought. The report may include safe reasoning surrogates only: policy summaries, route explanations, validation outputs, human notes, evidence fragment references, and classifier diagnostics.

After creating the package docs:
1. Add a short working-log section with today’s intended implementation sequence.
2. Do not modify unrelated package folders.
3. Do not move DEC-RECON-004 or DEC-RECON-006 unless the repo convention requires marking packages consumed; if you do, document exactly what moved.
```

## Prompt 2 — Implement DEC-RECON-007 in `kanon-dotnet`

```text
You are working in C:\dev\kanon-dotnet.

Implement DEC-RECON-007: persisted reconstructability report artifacts.

Context:
The system already has:
- GET /ontology/v0/decision-events/{decisionEventId}/reconstructability
- GET /ontology/v0/reconstructability/by-trace/{traceId}
- POST /ontology/v0/reconstructability/classify
- POST /ontology/v0/reconstructability/classify-batch

The new work must not break any of these. Existing classify/classify-batch behavior must remain compatible with DEC-RECON-004 and DEC-RECON-006.

Goal:
Add a persisted, versioned artifact layer so Kanon can answer:

    What did Kanon classify then?
    From which exact decision-event input?
    Under which classifier version?
    What report did it produce?

Artifact schema:
Use:

    ontogony-reconstructability-report-artifact-v1

Required artifact fields:
Design exact C# contracts following existing Kanon contract conventions, but include at minimum:

- schema
- artifactId
- decisionEventId
- decisionKind
- serviceOfOrigin
- sourceRef
- decisionEventHash
- classifierVersion
- classifiedAtUtc
- traceId
- runId
- correlationId
- relatedIds
- inputSchemaVersion
- reportSchemaVersion
- decisionEventSnapshot
- report
- ontogonyGovernanceStatus
- strictCompletenessPct
- createdAtUtc
- updatedAtUtc

Important:
- `decisionEventSnapshot` can be the full DecisionEventContract or a JSON-safe canonical snapshot, depending on existing serialization conventions.
- Include the full ReconstructabilityReportContract in the artifact.
- Do not store or expose hidden chain-of-thought. Preserve safe reasoning notice only.

Implementation steps:

1. Inspect existing reconstructability code:
   - ReconstructabilityEndpoints.cs
   - DecisionReconstructabilityService.cs
   - DecisionReconstructabilityClassifier.cs
   - DecisionEventContractValidator.cs
   - DecisionReconstructabilityContracts.cs
   - existing repository/persistence patterns
   - existing OpenAPI/route inventory/client coverage tests
   - authorization / ServerOnly / ProvenanceRead route conventions

2. Add classifier versioning.
   Add a small version provider or constant, for example:

       DecisionReconstructabilityClassifierVersion.Current = "kanon-decision-reconstructability-classifier-v1"

   Use a location consistent with the repo style.

3. Add canonical input hashing.
   Implement deterministic hashing for DecisionEventContract:
   - normalize JSON property ordering if possible
   - ignore no fields unless explicitly justified
   - include schemaVersion and all evidence fields
   - use SHA-256
   - output lowercase hex
   - add tests proving identical semantic payloads hash consistently

   Suggested method shape:

       DecisionEventCanonicalHasher.ComputeSha256(DecisionEventContract decisionEvent)

   If the repo already has canonical JSON / hashing helpers, reuse them.

4. Add artifact contracts.
   Suggested names:

       ReconstructabilityReportArtifactContract
       CreateReconstructabilityReportArtifactRequest
       CreateReconstructabilityReportArtifactsBatchRequest
       ReconstructabilityReportArtifactResponse
       ReconstructabilityReportArtifactsBatchResponse

   Minimal API shape should support:
   - single artifact creation
   - batch artifact creation
   - fetch by artifactId

   Prefer these routes unless an existing naming convention suggests better:

       POST /ontology/v0/reconstructability/report-artifacts
       POST /ontology/v0/reconstructability/report-artifacts/batch
       GET  /ontology/v0/reconstructability/report-artifacts/{artifactId}

   Optional but desirable:
       GET /ontology/v0/reconstructability/report-artifacts/by-decision-event/{decisionEventId}

5. Add persistence.
   Follow existing Kanon persistence patterns.
   Inspect whether Kanon uses EF Core, repository interfaces, migrations, in-memory fallbacks, or event/audit storage patterns.

   Required repository behavior:
   - upsert or idempotent create by deterministic artifactId
   - fetch by artifactId
   - query latest by decisionEventId if implemented
   - preserve createdAtUtc on idempotent re-create
   - update updatedAtUtc if the same artifact is re-created with the same hash/version/report

   Deterministic artifact ID:
   Use a stable id derived from:
       schema + decisionEventHash + classifierVersion
   Example:
       recon_art_<first 32 or 48 hex chars of sha256(schema + ":" + decisionEventHash + ":" + classifierVersion)>

   Do not include classifiedAtUtc in artifactId, otherwise identical input will not be idempotent.

6. Add service layer.
   Extend or add service methods:

       CreateReportArtifact(DecisionEventContract event, artifact metadata, cancellationToken)
       CreateReportArtifactsBatch(...)
       GetReportArtifact(...)
       GetLatestReportArtifactByDecisionEventId(...)

   The create flow should be:
       validate DecisionEventContract
       compute decisionEventHash
       classify using existing DecisionReconstructabilityClassifier
       build ReconstructabilityReportContract
       build artifact with classifierVersion and metadata
       persist artifact
       return artifact

   Source metadata request should allow:
       sourceRef
       runId
       traceId
       correlationId

   Also derive traceId/runId/correlationId from the DecisionEventContract when available, if the contract already contains them.

7. Add endpoints.
   Add routes in ReconstructabilityEndpoints.cs or an appropriate endpoint module.

   Authorization:
   These routes should use the same policy semantics as existing reconstructability endpoints:
   - ServerOnly / ProvenanceRead
   - do not make them less protected because they are “just reports”

   Validation behavior:
   - invalid event -> 422 DecisionEventNotClassifiable, same pattern as classify endpoint
   - missing batch decisionEvents -> 400
   - artifact not found -> 404 structured API error
   - success -> 200 or 201, consistent with repo conventions

8. OpenAPI / route inventory / compatibility docs.
   Update:
   - route catalog / inventory
   - OpenAPI baseline
   - client coverage JSON / compatibility manifest if present
   - CURRENT_STATE.md or equivalent state doc if repo convention requires it
   - docs/contracts if contract docs exist

9. Tests.
   Add focused tests for:
   - single artifact creation returns schema, artifactId, decisionEventHash, classifierVersion, report
   - batch artifact creation preserves input order
   - artifact fetch by artifactId
   - fetch missing artifact returns 404
   - invalid payload returns 422
   - same payload creates same decisionEventHash and same artifactId
   - changing relevant payload changes decisionEventHash
   - existing classify/classify-batch tests still pass
   - route inventory / OpenAPI guards pass

   Include at least one Allagma-shaped decision event fixture:
       run_operation
   Prefer adding:
       workflow_transition
       agent_tool_call
   if existing fixtures make this easy.

10. Run verification:
   Use repo-appropriate commands, but at minimum:

       dotnet build Kanon.sln -c Release --no-incremental
       dotnet test tests/Kanon.Tests/Kanon.Tests.csproj -c Release --no-build --filter "Reconstructability"

   Then run route/OpenAPI guard tests if they are not included in the filter.

11. Document results.
   Create or update an evidence doc, likely:

       docs/evidence/ONTOGONY_DECISION_RECONSTRUCTABILITY_SPINE_007_KANON_EVIDENCE.md

   Include:
   - routes added
   - artifact contract summary
   - test commands
   - pass/fail results
   - any environment-gated tests
   - known limitations

Scope boundaries:
- Do not implement Conexus decision-events here.
- Do not change the Allagma decision-events export contract unless a genuine incompatibility is found.
- Do not break frontend DEC-RECON-006 on-demand graph path.
- Do not make a large security/prod-readiness detour.
- Do not rename existing reconstructability routes.
```

## Prompt 3 — Wire DEC-RECON-007 into `ontogony-frontend`

```text
You are working in C:\dev\ontogony-frontend.

Implement frontend integration for DEC-RECON-007 after Kanon exposes persisted reconstructability report artifact endpoints.

Current state:
- DEC-RECON-004 proved live Allagma decision-events → Kanon classify-batch → panel.
- DEC-RECON-006 added Evidence Spine allagma.decisionEvent nodes with reconstructability overlay and per-node ReconstructabilityReportPanel.
- Current graph path still uses on-demand classify-batch.

Goal:
Progressively enhance the Evidence Spine and run decision reconstruction flows so they can use Kanon persisted report artifacts when available, while preserving the current on-demand classify-batch fallback.

Expected Kanon endpoints from DEC-RECON-007:
Confirm actual generated OpenAPI names, but expected routes are:

    POST /ontology/v0/reconstructability/report-artifacts
    POST /ontology/v0/reconstructability/report-artifacts/batch
    GET  /ontology/v0/reconstructability/report-artifacts/{artifactId}

Optional:
    GET /ontology/v0/reconstructability/report-artifacts/by-decision-event/{decisionEventId}

Important:
Use generated OpenAPI schemas. Do not hand-maintain DTOs if generated schemas exist.

Tasks:

1. Sync OpenAPI.
   Run the existing frontend OpenAPI workflow:
   - sync Allagma if needed
   - expand Kanon
   - generate schemas
   - run openapi check

   Use the existing scripts. Do not manually edit generated files except through the established generation flow.

2. Extend decisionReconstructabilityTypes.ts.
   Keep current aliases to generated Allagma/Kanon schemas.
   Add aliases for new Kanon artifact contracts:
   - ReconstructabilityReportArtifactContract
   - CreateReconstructabilityReportArtifactRequest
   - CreateReconstructabilityReportArtifactsBatchRequest
   - ReconstructabilityReportArtifactsBatchResponse
   or whatever exact generated schema names exist.

3. Add Kanon client methods.
   Extend src/kanon/api/kanonReconstructabilityClient.ts.

   Required:
       createReconstructabilityReportArtifactsBatch(settings, decisionEvents, metadata)
       getReconstructabilityReportArtifact(settings, artifactId)

   Optional:
       getLatestReconstructabilityReportArtifactByDecisionEventId(...)

   Use the same auth/header conventions as classifyDecisionEventsBatch:
   - bearer token from settings.kanon.serviceToken
   - actor headers
   - roles header
   - fromHttpResult / fromNullableHttpResult conventions

4. Preserve fallback behavior.
   The loader should prefer artifacts but fall back cleanly.

   For Evidence Spine:
       loadAllagmaRunDecisionReconstruction(...)
       should either:
         A. call report-artifacts/batch and build view from artifact.report
         B. if artifact endpoint fails/not configured, fall back to classify-batch

   Do not make Evidence Spine fail just because the artifact endpoint is unavailable.

   Suggested return shape:
       RunDecisionReconstructionViewDto rows can include:
           event
           report
           artifact? {
             artifactId
             decisionEventHash
             classifierVersion
             classifiedAtUtc
             sourceRef
           }

   If changing RunDecisionReconstructionViewDto is disruptive, add a parallel optional map:
       artifactByDecisionEventId

5. Extend EvidenceNodeReconstructability.
   Add optional artifact metadata:
       artifactId?: string
       decisionEventHash?: string
       classifierVersion?: string
       artifactSchema?: string
       sourceRef?: string

   Make sure all existing tests still pass.

6. Update appendAllagmaDecisionEventReconstructabilityGraph.
   When artifact metadata is present:
   - copy it into node.reconstructability
   - include artifact id in identifiers if a new identifier kind is warranted
   - include artifact sourceRef
   - add rawPreview artifact/report/event as needed

   Add new EvidenceIdentifierKind only if it improves navigation:
       reconstructabilityArtifactId

   If added, update labels and display priority.

7. Update UI.
   In EvidenceGraphNodeCard / EvidenceDecisionReconstructabilityPanel:
   - show artifactId if present
   - show decisionEventHash if present
   - show classifierVersion if present
   - show classifiedAtUtc from artifact/report
   - keep the existing PASS/WARN/FAIL badge and report panel behavior

   UI text should make the distinction clear:
       "Persisted artifact"
       "On-demand classification"
   depending on whether artifact metadata exists.

8. Tests.
   Add/extend tests for:
   - artifact metadata displayed on Evidence Spine node
   - artifact-backed path still renders ReconstructabilityReportPanel
   - fallback to classify-batch when artifact endpoint is unavailable
   - generated type aliases compile
   - openapi:check passes

   Existing tests to keep green:
   - decisionReconstructabilityContractDrift.test.ts
   - appendAllagmaDecisionEventReconstructabilityGraph.test.ts
   - buildRunDecisionReconstructionView tests
   - run-audit-journey / decision reconstructability smoke if feasible

9. E2E / smoke.
   Add a DEC-RECON-007 smoke test or extend DEC-RECON-004 smoke:
   - get Allagma decision-events for seeded run
   - create Kanon report artifacts batch
   - assert artifact count equals event count
   - open Evidence Spine URL:
         /system/evidence-spine?id={runId}&kind=allagmaRunId
   - assert Decision reconstructability section visible
   - assert artifact metadata visible if available

   Save artifact report where current smoke artifacts live:
       ontogony-platform/docker/local-working-system/artifacts/dec-recon-007-smoke-report.json
   or follow the established path convention.

10. Run verification:
   At minimum:

       npm run openapi:check
       npm run build
       npm test -- decisionReconstructability
       npm test -- appendAllagmaDecisionEventReconstructabilityGraph

   If stack is up:
       npx playwright test e2e/decision-reconstructability-docker-live.spec.ts --config=playwright.docker-local.config.ts

   If you add a new DEC-RECON-007 E2E spec, run that too.

11. Evidence doc.
   Create or update:

       docs/evidence/ONTOGONY_DECISION_RECONSTRUCTABILITY_SPINE_007_EVIDENCE.md

   Include:
   - whether artifact endpoint was used or fallback path was used
   - runId / traceId
   - eventCount / artifactCount / reportCount
   - worstGovernanceStatus
   - classifierVersion observed
   - test commands and results

Scope boundaries:
- Do not implement Conexus decision-events here.
- Do not remove the current classify-batch path.
- Do not block Evidence Spine on artifact persistence.
- Do not hand-edit generated schemas.
- Keep UI language clear that hidden chain-of-thought is not exposed.
```

## Prompt 4 — Full cross-repo closeout for DEC-RECON-007

```text
You are closing out DEC-RECON-007 across:

    C:\dev\kanon-dotnet
    C:\dev\ontogony-frontend
    C:\dev\ontogony-platform

Goal:
Produce a trustworthy closeout proving persisted reconstructability report artifacts work end to end.

Expected final state:
- Kanon can create persisted reconstructability report artifacts from exported DecisionEventContract payloads.
- Artifacts include decisionEventHash, classifierVersion, classifiedAtUtc, source ref, full report payload, and run/trace/correlation identifiers where available.
- Frontend can show artifact metadata in Evidence Spine decision-event nodes.
- Existing on-demand classify-batch path remains as fallback.
- DEC-RECON-004 and DEC-RECON-006 behavior remains intact.

Closeout tasks:

1. Check git status in each repo.
   Report uncommitted files before modifying anything.

2. Verify Kanon.
   Run:

       cd C:\dev\kanon-dotnet
       dotnet build Kanon.sln -c Release --no-incremental
       dotnet test tests\Kanon.Tests\Kanon.Tests.csproj -c Release --no-build --filter "Reconstructability"

   Also run route/OpenAPI/manifest guards if they are separate.

   Expected:
   - new artifact tests pass
   - existing classify/classify-batch tests pass
   - route inventory count updated intentionally
   - OpenAPI snapshot updated intentionally

3. Verify frontend.
   Run:

       cd C:\dev\ontogony-frontend
       npm run openapi:check
       npm run build
       npm test -- decisionReconstructability
       npm test -- appendAllagmaDecisionEventReconstructabilityGraph

   If there is a specific test command convention, use that.

4. Verify live stack if available.
   From docker/local-working-system:
   - ensure Allagma, Kanon, Conexus, frontend are healthy
   - use existing seeded run if ENV-SEED is still blocked
   - run DEC-RECON-007 smoke

   Expected smoke:
       Allagma GET decision-events
       Kanon POST report-artifacts/batch
       Kanon GET artifact by artifactId
       Evidence Spine page shows decision reconstructability section
       artifact metadata visible

   Save:
       docker/local-working-system/artifacts/dec-recon-007-smoke-report.json

   Include:
       schema
       recordedAtUtc
       runId
       traceId
       eventCount
       artifactCount
       reportCount
       worstGovernanceStatus
       classifierVersion
       sampleArtifactId
       sampleDecisionEventHash
       steps[]
       verdict

5. Update evidence docs.
   In kanon-dotnet:
       docs/evidence/ONTOGONY_DECISION_RECONSTRUCTABILITY_SPINE_007_KANON_EVIDENCE.md

   In ontogony-frontend:
       docs/evidence/ONTOGONY_DECISION_RECONSTRUCTABILITY_SPINE_007_EVIDENCE.md

   In ontogony-platform:
       docs/_incoming/_consumed/2026-05/ONTOGONY-DECISION-RECONSTRUCTABILITY-SPINE-007/README.md
       or move package to _consumed if that is the current repo convention.

   Evidence must distinguish:
   - tests actually run
   - tests not run
   - environment-gated failures
   - known unrelated blockers such as ENV-SEED topologyAuthorizationDecisionId

6. Check for regressions.
   Confirm:
   - DEC-RECON-004 smoke command still works or explain why it was not run.
   - DEC-RECON-006 Evidence Spine graph still shows decision-event nodes.
   - Existing classify-batch route still works.
   - No Conexus decision-events were added in this slice.

7. Commit plan.
   Recommend separate commits:

   kanon-dotnet:
       feat(reconstructability): persist report artifacts

   ontogony-frontend:
       feat(evidence): show reconstructability report artifacts

   ontogony-platform:
       docs(dec-recon): close persisted artifact evidence

   Do not commit unrelated local changes. If unrelated changes exist, list them separately.

8. Final response format.
   Provide:
   - summary by repo
   - routes added
   - artifact schema
   - exact commands run
   - exact pass/fail results
   - smoke artifact path
   - remaining work

Remaining work should list only:
   - ENV-SEED topology invariant if still present
   - CONEXUS-DECISION-EVENTS-001
   - any actual failed tests

Do not claim success for any command that was not actually run.
```

## Recommended scope for 007

The most important design decision is this:

```text
Keep classify/classify-batch as live/on-demand classification.
Add report-artifacts/report-artifacts-batch as the persisted, auditable path.
```

That avoids breaking DEC-RECON-004/006 while giving the system the audit-grade layer it now needs.
