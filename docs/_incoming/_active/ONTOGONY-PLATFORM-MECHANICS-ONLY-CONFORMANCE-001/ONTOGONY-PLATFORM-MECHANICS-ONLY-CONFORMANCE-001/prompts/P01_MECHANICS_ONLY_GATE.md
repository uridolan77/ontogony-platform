# P01 — Mechanics-only gate

Implement the Platform proposal gate.

Read `contracts/PLATFORM_MECHANICS_ONLY_PROPOSAL_GATE.md`.

Create docs, schema, fixtures, scripts, and tests so Platform additions are classified as:

- accepted platform mechanics;
- rejected product semantics;
- needs owner routing;
- needs schema versioning;
- needs consumer impact review.

Do not use lexical scanning alone as the final authority. Produce a report with suggested owner repo.
