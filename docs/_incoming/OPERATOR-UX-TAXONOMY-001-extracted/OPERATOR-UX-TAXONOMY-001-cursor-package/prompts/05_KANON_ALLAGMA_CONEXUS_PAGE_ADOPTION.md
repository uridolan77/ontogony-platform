# Cursor prompt — 05 Kanon/Allagma/Conexus page adoption

Migrate product pages to the shared taxonomy without broad workflow rewrites.

## Kanon

- Rename ambiguous `Gateway health` to the right dimension: Kanon API health/readiness or Conexus gateway health.
- Normalize actor/role capability wording using capability statuses.
- Show domain-pack/source-binding data source and authority.
- Move route/API implementation notes into developer details.
- Use full ontology ID and clear configured vs active/live labels.

## Allagma

- Remove unlabeled `unknown` from run cards.
- Structure `stream output withheld` with labels.
- Replace `Failed runs (sample)` with scoped metric language.
- Render runtime posture with shared blocked/degraded/usability states.

## Conexus

- Distinguish model purpose, model alias, provider model, provider posture.
- Show fake vs real provider mode as data/authority context.
- Route preview should show selected route/fallback readiness using topology/provider states.
