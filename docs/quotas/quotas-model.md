# Quotas model

`Ontogony.Quotas` defines mechanical quota records and decisions.

Conexus.NET should own policy: which quota applies, which metric is consumed, which project/user/provider is subject to the limit, and which error shape is returned.
