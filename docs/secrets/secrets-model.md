# Secrets model

`Ontogony.Secrets` defines secret references, safe display metadata, fingerprints, and protection ports.

Production Conexus.NET should implement `ISecretProtector` using a real vault or encryption mechanism. The included development protector is local/test only.
