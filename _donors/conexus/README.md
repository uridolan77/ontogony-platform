# `_donors/conexus/` — historical donor archive

> **QUARANTINE:** Not current Ontogony behavior. Legacy **Python Conexus** gateway samples preserved for archaeology only.
>
> **Model gateway** lives in **conexus-dotnet**. See [`../README.md`](../README.md) and [`docs/CURRENT_STATE.md`](../../docs/CURRENT_STATE.md).

Do not reference from solution builds, CI, package graphs, or active examples. Python bytecode must not be committed (`__pycache__/`, `*.pyc`).
